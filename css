* {
    margin: 0;
    padding: 0;
}

body {
    background-color: antiquewhite;
    font-family: Arial, sans-serif;
}

.header {
    background-color: brown;
    text-align: center;
    padding-top: 15px;
    
}

.banner {
    background-image: url("arquivos/");
    background-position: center;
    background-size: cover;
    height: 300px;
}



.card {
    width: 500px;
    margin: auto;
    padding: 50px;
    background-color: rgb(250, 238, 222);
    margin: 50px auto;
    border-radius: 5px;
    font-weight: 300;
}

h1 {
    text-align: center;
    font-weight: 400;
    font-size: 20px;
    color: #000000;
    margin-bottom: 20px;
}

li {
    list-style-position: inside;
    line-height: 35px;
}

ul {
    margin-bottom: 40px;
}

.botao {
    margin-top: 50px;
    text-decoration: none;
    background-color: brown;
    color: white;
    padding: 15px;
    font-weight: 400;
    font-size: 20px;
    border-radius: 30px;
}

.botao:hover {
    background-color: brown;
}

.titulo-receita {
    font-weight: 700;
    font-size: 30px;
    margin-top: 30px;
}

.link-footer {
    font-weight: 700;
    text-decoration: none;
    color: white;
}

ul {
    display: flex;
    justify-content: space-between;
    list-style-type: none;
    padding: 16px;
}

/* Estiliza links */ 
a {
    color: black;
    width: 100px;
    margin: auto;
    padding: 10px;
    text-align: center; /* Centraliza o texto no link. */
    text-decoration: none; /* Remove o sublinhado padrão. */
}

/* Estiliza botões de login */
.login-button {
    display: flex;
    align-items: center;
}

.login-button button {
    border: none;
    background-color: brown;
    padding: 0.4rem 1rem;
    border-radius: 5px;
    cursor: pointer;
}

.login-button button:hover {
    background-color: aliceblue;
}

.login-button button a {
    text-decoration: none;
    font-weight: 500;
    color: aliceblue;
}

/* Estiliza o cabeçalho do formulário */
.form-header h1::after {
    content: '';
    display: block;
    width: 5rem;
    height: 0.3rem;
    background-color: aliceblue;
    margin: 0 auto;
    position: absolute;
    border-radius: 10px;
}

/* Estiliza grupos de entrada */
.input-group {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    padding: 1rem 0;
}

/* Estiliza caixas de entrada */
.input-box input {
    margin: 0.6rem;
    padding: 0.8rem 1.2rem;
    border: none;
    border-radius: 10px;
    box-shadow: 1px 1px 6px #000;
    width: 100%; /* Faz os campos de entrada ocuparem 100% do espaço disponível. */
}

/* Estiliza o rodapé */
.footer {
    background-color: antiquewhite;
    height: 100px;
    text-align: center;
    text-align: center;
    display: flow-root;
    align-items: center; /* Centraliza verticalmente o conteúdo do rodapé. */
    padding: 600; /* Adiciona preenchimento nas laterais para espaçamento. */
    
}




