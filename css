/* Reset de margens e preenchimentos para todos os elementos */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box; /* Adicionei esta linha para incluir borda e preenchimento no cálculo do tamanho do elemento. */
}

body {
    background-color: bisque;
}

.header {
    background-color: brown;
    height: 65px;
    display: flex;
    align-items: center; /* Centralize verticalmente o conteúdo do cabeçalho. */
    justify-content: space-between; /* Crie espaço entre os elementos internos do cabeçalho. */
    padding: 0 20px; /* Adicionei preenchimento nas laterais para espaçamento. */
}

.banner {
    /* Certifique-se de definir uma altura e um tamanho de plano de fundo apropriados. */
    background-position: center;
    background-size: cover;
    height: 300px; /* Defina a altura apropriada para o banner. */
}

.card {
    width: 200px; /* Adicione "px" para especificar a unidade de largura. */
    margin: auto;
    padding: 20px;
    background-color: rgb(252, 230, 204);
    box-shadow: 1px 1px 6px #000;
    border-radius: 10px;
}

h1 {
    font-weight: 400;
    font-size: 20px;
    color: rgb(0, 0, 0);
}

li {
    list-style-position: outside;
}

ul {
    display: flex;
    justify-content: space-between;
    list-style-type: none;
    padding: 16px;
}

a {
    color: black;
    width: 100px; /* Adicione "px" para especificar a unidade de largura. */
    margin: auto;
    padding: 10px;
    text-align: center; /* Centralize o texto no link. */
    text-decoration: none; /* Remova a sublinhado padrão. */
}

.login-button {
    display: flex;
    align-items: center;
}

.login-button button {
    border: none;
    background-color: brown;
    padding: 0.4rem 1rem;
    border-radius: 5px;
    cursor: pointer;
}

.login-button button:hover {
    background-color: aliceblue;
}

.login-button button a {
    text-decoration: none;
    font-weight: 500;
    color: aliceblue;
}

.form-header h1::after {
    content: '';
    display: block;
    width: 5rem;
    height: 0.3rem;
    background-color: aliceblue;
    margin: 0 auto;
    position: absolute;
    border-radius: 10px;
}

.input-group {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    padding: 1rem 0;
}

.input-box input {
    margin: 0.6rem;
    padding: 0.8rem 1.2rem;
    border: none;
    border-radius: 10px;
    box-shadow: 1px 1px 6px #000;
    width: 100%; /* Faça os campos de entrada ocuparem 100% do espaço disponível. */
}

.footer {
    background-color: brown;
    height: 65px;
    text-align: center;
    display: flex;
    align-items: center; /* Centralize verticalmente o conteúdo do rodapé. */
    padding: 0 20px; /* Adicione preenchimento nas laterais para espaçamento. */
}
